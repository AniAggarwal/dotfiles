" Config for neoterm

" Set default location of new terminals to vertical
let g:neoterm_default_mod = 'vertical'

" Use relative paths (so ./foo works in code)
let g:neoterm_use_relative_path = 1

" TODO get conda working
let g:neoterm_repl_python = 'conda'
let g:neoterm_repl_python_venv = 'conda'
